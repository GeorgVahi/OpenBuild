interface:
  display_name: "Build"
  short_description: "Plan, refine, run, and review implementation specs"
  default_prompt: "Use this Build skill in full mode to turn an idea into a reviewed implementation."
policy:
  allow_implicit_invocation: false
